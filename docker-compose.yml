services:
  freeipa:
    image: freeipa/freeipa-server:almalinux-9
    container_name: freeipa
    hostname: ipa.packops.local
    read_only: true
    tmpfs:
      - /run
      - /tmp
    volumes:
      - ipa-data:/data:Z
      - /sys/fs/cgroup:/sys/fs/cgroup:rw          # ← THIS LINE WAS MISSING
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=0
    environment:
      - PASSWORD=Admin@123
      - IPA_SERVER_IP=172.31.6.95
    ports:
      - "53:53/tcp"
      - "53:53/udp"
      - "80:80"
      - "443:443"
      - "389:389"
      - "636:636"
      - "88:88/tcp"
      - "88:88/udp"
      - "464:464/tcp"
      - "464:464/udp"
      - "123:123/udp"
    restart: unless-stopped

volumes:
  ipa-data: