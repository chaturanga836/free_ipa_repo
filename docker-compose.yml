# docker-compose.yml  ←  THIS IS THE ONE THAT WORKS 100%
services:
  freeipa:
    image: freeipa/freeipa-server:almalinux-9
    container_name: freeipa
    hostname: ipa.packops.local
    read_only: true
    tmpfs:
      - /run
      - /tmp
    volumes:
      - ipa-data:/data:Z
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=0
    environment:
      - PASSWORD=Admin@123
      - IPA_SERVER_IP=172.31.6.95
    ports:
      - "80:80"
      - "443:443"
      - "389:389"
      - "636:636"
      - "88:88"
      - "88:88/udp"
      - "464:464"
      - "464:464/udp"
      - "53:53"
      - "53:53/udp"
      - "123:123/udp"
    restart: unless-stopped

volumes:
  ipa-data: